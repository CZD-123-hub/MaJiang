# 九江红中四人自博弈实施计划

1. 新增失败测试，覆盖固定种子可复现、当前座位手牌隔离、自动打完一局、批量结果统计。
2. 新建 `jiujiang_ai/self_play.py`，实现牌局状态、牌堆发放、决策请求数据构建和合法动作生成。
3. 实现弃牌后的胡/碰/明杠响应、持牌阶段的胡/暗杠/补杠/听/弃牌决议，以及流局和庄家轮转。
4. 对接 `round_end`，输出单局概要和批量汇总。
5. 新增 `examples/jiujiang_self_play.py` 命令行入口。
6. 运行自博弈测试、现有九江测试与固定种子的小样本命令，修正发现的问题。
