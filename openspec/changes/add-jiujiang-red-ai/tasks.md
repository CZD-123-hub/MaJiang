## 1. 测试骨架

- [x] 1.1 新增九江红中特定测试文件，覆盖牌集、规则、手牌拆分、评估器和 API 行为。
- [x] 1.2 编写九江合法牌集与非九江字牌拒绝的失败测试。
- [x] 1.3 编写红中约束的失败测试：红中万能牌、红中不能碰杠、四红中胡牌优先。

## 2. 包结构

- [x] 2.1 新建独立 `jiujiang_ai/` 包，并添加 `__init__.py`。
- [x] 2.2 新增 `tiles.py`，实现九江牌常量、校验、格式化和剩余牌统计工具。
- [x] 2.3 新增 `actions.py` 或 `rules.py`，集中定义动作常量和九江动作合法性校验。

## 3. 手牌分析

- [x] 3.1 实现平胡手牌分析，先将红中从普通数牌中分离。
- [x] 3.2 实现普通数牌拆分：刻子、顺子、对子、搭子、孤张。
- [x] 3.3 实现红中万能牌消耗逻辑，用于降低缺将、缺面子的结构距离。
- [x] 3.4 实现四红中直接胡检测。
- [x] 3.5 通过无红中、一张红中、多张红中、四红中等手牌分析测试。

## 4. 弃牌评估

- [x] 4.1 新增弃牌评估器，根据向听数、面子结构、搭子结构、有效进张、孤张和红中使用情况为候选弃牌评分。
- [x] 4.2 将弃牌选择限制在 `action_cards["7"]` 提供的候选牌中。
- [x] 4.3 添加测试，证明在其他因素相同时，孤张弃牌优于拆掉已完成面子。
- [x] 4.4 添加测试，证明较低向听数和更多有效进张会被优先选择。

## 5. 操作选择

- [x] 5.1 实现动作类型 `4` 可用时的胡牌优先选择。
- [x] 5.2 实现不支持、非法或低价值动作时的过牌兜底。
- [x] 5.3 实现非红中牌碰牌评估，使用操作前后价值比较。
- [x] 5.4 实现非红中牌合法杠选择，且胡牌优先级高于杠。
- [x] 5.5 添加测试，确保永不选择吃牌，且拒绝红中碰/杠。

## 6. 公共 API

- [x] 6.1 新增 `api.py`，实现 `get_action(data)`，返回 `(action_type, action_card)` 或等价字典格式。
- [x] 6.2 新增 `round_end(data)`，接收对局结束数据并返回成功确认。
- [x] 6.3 使用需求文档中的代表性输入数据，添加输出结构和行为测试。

## 7. 验证

- [x] 7.1 运行完整 Python 测试套件并修复失败。
- [x] 7.2 手动运行 discard、hu、peng、gang、pass 场景的 `get_action` 样例调用。
- [x] 7.3 对照实现复查 OpenSpec 文档，如行为变化则同步更新 tasks/specs。
