[project]
name = "mortal-gbmj-res2net50"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "torch",
    "tqdm",
    "tensorboard",
    "colorlog",
]

[tool.pyright]
pythonVersion = "3.11"
